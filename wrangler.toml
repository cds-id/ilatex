# Cloudflare Pages config for the ilatex-editor demo (accents / decorations /
# matrices playground). Build output is produced by `npm run build` into
# ../dist-demo.
name = "ilatex-demo"
compatibility_date = "2026-07-07"
pages_build_output_dir = "dist-demo"
